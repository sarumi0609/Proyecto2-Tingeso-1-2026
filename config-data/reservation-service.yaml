server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://eureka-service:8761/eureka
  instance:
    prefer-ip-address: true

spring:
  datasource:
    url: jdbc:postgresql://${DB_RESERVATION_HOST:localhost}:5432/${DB_RESERVATION_NAME:reservationdb}
    username: ${DB_RESERVATION_USER:postgres}
    password: sarumi
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false

scheduling:
  enabled: true
