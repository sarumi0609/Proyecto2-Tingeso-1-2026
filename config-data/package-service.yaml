server:
  port: ${PORT:${SERVER_PORT:0}}

spring:
  datasource:
    url: jdbc:postgresql://${DB_PACKAGE_HOST:localhost}:5432/${DB_PACKAGE_NAME:db_packages}
    username: ${DB_PACKAGE_USER:postgres}
    password: ${DB_PACKAGE_PASS:sarumi}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
  instance:
    instance-id: ${spring.application.name}:${random.value}
