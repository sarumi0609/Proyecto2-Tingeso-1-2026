# ============================================================
# PostgreSQL para reservation-service
# ============================================================
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: reservation-postgres-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: reservation-postgres
spec:
  selector:
    matchLabels:
      app: reservation-postgres
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: reservation-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: reservation-db-secret
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: reservation-db-secret
                  key: password
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: reservation-db-secret
                  key: dbname
          volumeMounts:
            - name: storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: reservation-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: reservation-postgres
spec:
  selector:
    app: reservation-postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None
