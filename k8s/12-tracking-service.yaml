# ============================================================
# Tracking Service — M6, puerto dinámico, ClusterIP
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tracking-service
spec:
  selector:
    matchLabels:
      app: tracking-service
  replicas: 1
  template:
    metadata:
      labels:
        app: tracking-service
    spec:
      containers:
        - name: tracking-service
          image: sarumimope/tracking-service:latest
          env:
            - name: SPRING_CONFIG_IMPORT
              value: "configserver:http://config-service:8888"
            - name: EUREKA_URL
              value: "http://eureka-service:8761/eureka"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: tracking-service
spec:
  selector:
    app: tracking-service
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
